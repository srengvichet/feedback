{
  "properties": [
    {
      "name": "app.base-url",
      "type": "java.lang.String",
      "description": "Base URL used for QR code generation. Set to the server LAN IP so mobile devices can scan the codes (e.g. http://192.168.1.37:8080). Leave empty to auto-detect from the request."
    }
  ]
}
